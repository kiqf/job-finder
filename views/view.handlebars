<div id="job-view-container" class="container d-flex flex-column vh-100 justify-content-center align-items-center">
  <div class="row text-center w-100">

    <div class="position-relative">
      <a class="btn btn-primary position-absolute start-0 mt-3 ms-3" href="/">Voltar</a>
    </div>
    <!-- Imagem da Empresa 
    <div id="img-container" class="col-md-12">
      <img src="/public/img/icon.svg" alt="Company" class="img-fluid mb-3" style="max-width: 150px;">
    </div>
-->
    <!-- Detalhes da Vaga -->
    <div class="col-md-12">
      <h1 class="main-color display-4">{{job.dataValues.title}}</h1>

      <p class="bold lead mt-4">Quem eles estão buscando:</p>
      <p class="job-desc mb-4 text-muted">{{job.dataValues.description}}</p>

      <p><span class="bold">O salário informado é:</span> 
        <span class="text-success">R${{job.dataValues.salary}}</span>
      </p>

      <p>Para trabalhar na <span class="bold main-color">{{job.dataValues.company}}</span>, envie um e-mail para:
        <a href="mailto:{{job.dataValues.email}}" class="text-decoration-none text-primary">
          {{job.dataValues.email}}
        </a>
      </p>
    </div>
  </div>
</div>